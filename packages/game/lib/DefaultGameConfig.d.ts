import { IGameConfig } from "./IGameConfig";
export declare class DefaultGameConfig implements IGameConfig {
    roundTimer: number;
    pickTimer: number;
    maxPoints: number;
    packs: never[];
    maxPlayers: number;
    constructor(roundTimer?: number, pickTimer?: number, maxPoints?: number, packs?: never[], maxPlayers?: number);
}
//# sourceMappingURL=DefaultGameConfig.d.ts.map